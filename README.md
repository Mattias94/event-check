# Event-check

Monorepo starter with `frontend/` and `backend/` folders.

Quick start

1. Initialize git and make initial commit:

```bash
git init
git add .
git commit -m "chore: initial repo structure"
```

2. Create a `.env` from `.env.example` and fill values.

3. Enter `frontend` or `backend` to scaffold each app.
